# EvoAlign Context Lattice v0.1
# =============================
# Explicit capability semantics for context_class IDs

version: "0.1.0"
description: "EvoAlign Context Lattice v0.1 — capability semantics for context_class IDs"

dimensions:
  tool_access:
    type: set
    atoms: ["web", "email"]
    top: "*"
    bottom: []

  autonomy_level:
    type: ordered_enum
    order: ["none", "assistant", "act_with_confirm", "act"]
    top: "*"
    bottom: "none"

contexts:
  any:
    tool_access: "*"
    autonomy_level: "*"

  no_tools:
    tool_access: []
    autonomy_level: "assistant"

  tool_access:any:
    tool_access: "*"
    autonomy_level: "assistant"

  tool_access:limited:
    tool_access: ["web"]
    autonomy_level: "assistant"

  tool_access:web+email:
    tool_access: ["web", "email"]
    autonomy_level: "assistant"

metadata:
  created_at: "2025-01-15T00:00:00Z"
  rfc_reference: "RFC-CTX-0001"
  approvals:
    - role: "Technical Safety Lead"
      signature: "sig_ctx_abc123..."
      timestamp: "2025-01-15T10:30:00Z"
    - role: "Security Lead"
      signature: "sig_ctx_def456..."
      timestamp: "2025-01-15T10:45:00Z"
